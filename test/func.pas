program factorial(fac);
	var
		fact : Integer;
	begin
	{ Sort using
        bubble sort. }

		if (num) then
        	{bubble sort. }
			fact := 11
		else
			fact := num * fact(num);

		FOR ii := 1 TO size DO
			begin
	            writeln("arr[ii]");
	            fact := num * fact(num);
	        end
	end

end.